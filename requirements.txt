# Fog-based AI Network Anomaly Detection System
# Python 3.10+ required

# Core dependencies
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0

# SDN and Network
ryu>=4.34
mininet>=2.3.0

# Machine Learning (optional - for advanced models)
tensorflow>=2.13.0; platform_system != "Windows"
# tensorflow-macos>=2.13.0; platform_system == "Darwin"

# Network capture (optional - if Tshark not available)
# pyshark>=0.6  # Alternative to Tshark

# Utilities
python-dateutil>=2.8.0

# Logging and monitoring
matplotlib>=3.7.0
seaborn>=0.12.0

# Note: Tshark must be installed separately via system package manager
# On Kali Linux: sudo apt-get install tshark
# On Ubuntu/Debian: sudo apt-get install tshark
# On macOS: brew install wireshark

